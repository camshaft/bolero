[package]
name = "bolero-engine"
version = "0.4.1"
authors = ["Cameron Bytheway <bytheway.cameron@gmail.com>"]
description = "fuzz and property testing framework"
homepage = "https://github.com/camshaft/bolero"
repository = "https://github.com/camshaft/bolero"
keywords = ["testing", "quickcheck", "property", "fuzz", "fuzzing"]
license = "MIT"
edition = "2018"
readme = "../README.md"

[features]
default = ["std", "rand", "panic_hook"]
std = ["alloc", "bolero-generator/std", "bolero-instrument/std"]
alloc = ["bolero-generator/alloc", "bolero-instrument/alloc"]
panic_hook = []

[dependencies]
bolero-generator = { version = "0.4", path = "../bolero-generator", default-features = false }
bolero-instrument = { version = "0.4", path = "../bolero-instrument", default-features = false }
failure = "0.1"
lazy_static = "1"
pretty-hex = "0.1.1"
rand = { version = "0.7", optional = true }

[dev-dependencies]
rand = "0.7"
