[package]
name = "cargo-bolero"
version = "0.1.1"
authors = ["Cameron Bytheway <bytheway.cameron@gmail.com>"]
description = "cargo command for running bolero fuzz tests"
homepage = "https://github.com/camshaft/bolero"
repository = "https://github.com/camshaft/bolero"
keywords = ["testing", "fuzz"]
license = "MIT"
edition = "2018"

[dependencies]
bolero-afl = { version = "0.1", path = "../bolero-afl", features = ["bin"] }
bolero-honggfuzz = { version = "0.1", path = "../bolero-honggfuzz", features = ["bin"] }
failure = "0.1"
structopt = "0.3"
tempfile = "3"
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
